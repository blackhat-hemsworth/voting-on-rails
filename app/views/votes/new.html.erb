<h1>Make your vote!</h1>

<%= form_with model: @vote do |form| %>
  Selections:
  <ul>
    <%= form.fields_for :selections do |selection_form|%>
      <li>
        <%= selection_form.select :selection, @vote.choices %>
      </li>
    <% end %>
  </ul>
  <div>
    <%= form.submit %>
  </div>
<% end %>
